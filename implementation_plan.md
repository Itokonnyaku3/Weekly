# Weekly Tracker 実装予定リスト

このドキュメントは、[project-weekly-tracker.html](file:///c:/Users/13122180\AG/project-weekly-tracker.html) に今後実装したい機能や修正内容を管理するためのリストです。

## 🏗️ 実装中 / テスト中 (In Progress)

- [ ] **画像データのGitHub外出し対応** 🆕
  - ノート挿入時や貼り付け時に、画像をGitHubの `images/` フォルダに自動アップロードする機能を実装。
  - `data.json` 内には画像URLのみを保持し、JSONの肥大化を抑制。
- [ ] **パフォーマンスの劇的改善** 🆕
  - **アウトライン描画の最適化**: 構造が変わらない移動や入力では `innerHTML` を書き換えない。
  - **グリッド描画の高速化**: デイリータスクの抽出・HTML生成をキャッシュ化。
- [ ] **既存画像の移行ツール** 🆕
  - すでに `data.json` に埋め込まれている `base64` 画像を GitHub にアップロードし、リンクに置換する機能。

## 📋 未実装・今後のタスク (Todo)

- [ ] **スマホ対応（モバイル表示の最適化）**
  - Webへのアップロード手順の確立、および機能を限定したシンプルなスマホ用レイアウトの実装。
- [ ] **Weekly フォーカスモード**
  - 特定のプロジェクトや週に集中して表示・編集できるモード。
- [ ] **UIの改善**
  - ボタンの配置や余白、配色のさらなるブラッシュアップ。
- [ ] **グリッドテーブルの幅調整ハンドル**
  - 列の境界線などをドラッグして幅を直感的に調整できるハンドル（リサイズ機能）の追加。

## ✅ 完了したタスク (Done)

- [x] **デイリーノートとWeeklyの連携強化**
- [x] **アプリ化（Edge/PWA対応）**
- [x] **プライベートモードとお仕事モードの切り替え**
- [x] **ノート編集時のフォーカス維持**
- [x] **プロジェクト間のアイテム移動とタグ連動**
- [x] **同期エラー（SHA不一致等）の自動回復**

---
📝 **メモ/方針:**
* 画像の外出しにより、`data.json` のサイズ制限（1MB以上のContents API制限等）を根本的に解決します。
* 描画の最適化により、大量のノートがある状態でも入力の遅延をなくします。
